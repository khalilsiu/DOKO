import { Typography, withStyles } from '@material-ui/core';

const SectionLabel = withStyles({
  root: {
    fontFamily: 'OpenSans',
    fontWeight: 700,
  },
})(Typography);

export default SectionLabel;
