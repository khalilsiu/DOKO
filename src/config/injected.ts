import { InjectedConnector } from '@web3-react/injected-connector';

// 1 = Ethereum Mainnet
// 42 = Kovan Testnet
// NOTE: support wallet connection for networks of all chain IDs
export const injected = new InjectedConnector({});
